#version 450

// Dummy shader for reflecting the size and layouts of standard uniform buffers.
// Since these uniform buffers have a the "shared" layout flag they don't need to be used within this shader.

#include "stdheader.glsl"

layout (local_size_x = 1, local_size_y = 1, local_size_z = 1) in;
void main()
{
}